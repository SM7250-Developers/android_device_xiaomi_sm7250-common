allow platform_app zygote:unix_stream_socket { getopt };

allow platform_app {
    nfc_service
    }:service_manager find;
allow platform_app {
    vendor_audio_prop
    vendor_display_prop
    }:file { read map open getattr };
